"""
Feature engineering logic.
In a production app, this would process raw NDVI time series into mean, trend, etc.
For this prototype, our dataset.csv already contains these features.
"""

def process_features(raw_data):
    # Placeholder for logic that converts raw GEE output to model features
    return raw_data
